---
title: Downloads and checksums
description: 0d3sa binary downloads, release version, platform matrix, SHA-256 checksums, and manual install snippets.
content_type: reference
---

import downloads from '../../../../data/downloads.json';

# Downloads and checksums

The latest 0d3sa release is **{downloads.version}**, released on **{downloads.released}**. Binaries are published as GitHub Release assets at `yakovkhalinsky/eden-releases`.

Each release ships two binaries:

- **`od3sa-memory`** — the full CLI with MCP server, memory tools, sync, and pairing.
- **`od3sa-relay`** — a dedicated, lightweight relay server with no MCP or memory subcommands.

## Quick install

The recommended installer defaults to `od3sa-memory`. Pass a binary name to install `od3sa-relay` instead:

```bash
# Default: install od3sa-memory
curl -fsSL https://0d3sa.com/memory/install.sh | sh

# Install the dedicated relay binary
curl -fsSL https://0d3sa.com/memory/install.sh | sh -s od3sa-relay
```

The installer places the binary at `~/.local/bin/<binary-name>`.

export const PlatformTable = ({ binary }) => {
  const platforms = downloads.binaries[binary].platforms;
  return (
    <table>
      <thead>
        <tr>
          <th>Platform</th>
          <th>Architecture</th>
          <th>Filename</th>
          <th>Size</th>
          <th>SHA-256</th>
          <th>Download</th>
        </tr>
      </thead>
      <tbody>
        {Object.entries(platforms).map(([key, p]) => (
          <tr key={key}>
            <td>{p.os}</td>
            <td>{p.arch}</td>
            <td><code>{p.filename}</code></td>
            <td>{p.sizeBytes > 0 ? `${(p.sizeBytes / 1024 / 1024).toFixed(1)} MB` : '—'}</td>
            <td>{p.sha256 === 'PUBLISHED_AT_RELEASE' ? <em>published at release</em> : <code>{p.sha256}</code>}</td>
            <td><a href={p.downloadUrl}>download</a></td>
          </tr>
        ))}
      </tbody>
    </table>
  );
};

## `od3sa-memory`

<PlatformTable binary="memory" />

## `od3sa-relay`

<PlatformTable binary="relay" />

## Manual install

If you prefer to download and verify manually, replace `BINARY` with `od3sa-memory` or `od3sa-relay` and `PLATFORM` with your platform key from the tables above:

```bash
VERSION={downloads.version}
BINARY=od3sa-memory
PLATFORM=linux-amd64
FILENAME=${BINARY}-${PLATFORM}

wget https://github.com/yakovkhalinsky/eden-releases/releases/latest/download/${FILENAME}
wget https://github.com/yakovkhalinsky/eden-releases/releases/latest/download/${FILENAME}.sha256
sha256sum -c ${FILENAME}.sha256

mkdir -p ~/.local/bin
mv ${FILENAME} ~/.local/bin/${BINARY}
chmod +x ~/.local/bin/${BINARY}
```

## Release build targets

From the `0d3sa` monorepo. The two binaries are separate projects with their own
Makefiles, so each is built independently and writes to its own `dist/`:

```bash
make -C apps/memory release # od3sa-memory for linux-amd64/arm64, darwin-amd64/arm64
make -C apps/relay release  # od3sa-relay for linux-amd64/arm64, darwin-amd64/arm64
```

This produces `apps/memory/dist/od3sa-memory-<platform>` and
`apps/relay/dist/od3sa-relay-<platform>` for each supported platform.

## Verify the binary

After installing:

```bash
od3sa-memory version
od3sa-memory health
```

For `od3sa-relay`:

```bash
od3sa-relay --db /var/lib/relay/relay.db --addr 127.0.0.1:8787 &
curl http://localhost:8787/health
```

## See also

- [Connect Claude Code](/memory/tutorials/connect-claude-code/)
- [Getting started](/memory/getting-started/)
- [Run your own relay server](/relay/how-to/run-relay-server/)
- [GitHub Releases](https://github.com/yakovkhalinsky/eden-releases/releases/latest)
